
spring.flyway.enabled=true
app.frontend.url=http://localhost:3000
JWT_SECRET=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
JWT_EXPIRATION=86400000
logging.level.org.springframework.security=DEBUG

spring.jpa.hibernate.ddl-auto=validate
SENDGRID_API_KEY=SG.mock_sendgrid_key_for_testing

logging.level.root=INFO
logging.level.org.springframework.web=ERROR
logging.level.com.fractal=DEBUG

GOOGLE_CLIENT_ID=mock-google-client-id
GOOGLE_CLIENT_SECRET=mock-google-client-secret
# Add these so Spring doesn't complain about placeholders in main properties
spring.security.oauth2.client.registration.google.client-id=mock-google-client-id
spring.security.oauth2.client.registration.google.client-secret=mock-google-client-secret


spring.application.name=fractal

# Database Configuration
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/todo_db}
spring.datasource.username=${DB_USERNAME:todo_user}
spring.datasource.password=${DB_PASSWORD:password}

# Flyway (Database Migration)
spring.flyway.baseline-on-migrate=true

# JPA / Hibernate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# OAuth2 Google Configuration (We will fill these later)
logging.level.org.springframework.web.servlet.DispatcherServlet=DEBUG

app.jwt.secret=super-long-random-secret-key
app.jwt.expiration=86400000